-- Кадры, отсеянные ситом по крючку, считаем отдельно от мусора и брака.
--
-- Без этого разбор прогона врал второй раз подряд: 16.08 партия честно
-- отсеялась ситом (четыре слабых кадра, один без крючка, один сорван
-- сбоем Gemini), а бот отчитался «все отсеяны как брак, либо не
-- сгенерировались подписи» - и это выглядит как поломка, хотя система
-- отработала ровно как задумано.
alter table if exists collect_runs
  add column if not exists hookless integer not null default 0;
